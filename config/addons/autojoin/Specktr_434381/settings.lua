require('common');

local settings = T{ };
settings["decline"] = T{ };
settings["join"] = T{ };
settings["ignore"] = T{ };
settings["mode"] = 2;
settings["join"][1] = "steraru";
settings["join"][2] = "Sternaru";
settings["join"][3] = "Sterntwo";

return settings;
