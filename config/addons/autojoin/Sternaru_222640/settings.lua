require('common');

local settings = T{ };
settings["decline"] = T{ };
settings["join"] = T{ };
settings["ignore"] = T{ };
settings["mode"] = 2;
settings["join"][1] = "Specktr";
settings["join"][2] = "Shyaru";
settings["join"][3] = "Tarubaker";

return settings;
